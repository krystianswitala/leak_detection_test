#ifndef SECOND_MODULE_HPP
#define SECOND_MODULE_HPP

#include <cstddef>
#include <cstdint>

uint8_t* create_buffer(size_t size);
void buffer_operations(uint8_t* buffer, size_t size);

#endif // SECOND_MODULE_HPP
